node_modules
dist
.DS_Store
*.local
.env.local
tsconfig.tsbuildinfo
.npmrc

# Screenshot output — for looking at, not for committing.
screenshots/
public/logos/*

# Share cards from scripts/build-og.mjs — uploaded to the bucket, never committed.
og-out/
